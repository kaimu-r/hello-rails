<h1>スキル一覧</h1>

<ul id="skills">
  <% @skills.each do |skill| %>
    <li>
      <%= link_to skill.name, admin_skill_path(skill) %>
    </li>
  <% end %>
</ul>

<%= link_to "新しいスキルを作成", new_admin_skill_path %>
