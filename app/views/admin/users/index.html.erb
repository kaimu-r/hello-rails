<h1>ユーザー一覧</h1>

<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.full_name, admin_user_path(user) %>
        </li>
    <% end %>
</ul>

<%= link_to "新しいユーザーを作成", new_admin_user_path %>
